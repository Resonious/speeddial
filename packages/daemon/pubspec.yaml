name: speeddial_daemon
description: SpeedDial daemon - agent session orchestration over ACP with a WebSocket JSON-RPC API.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  speeddial_protocol:
    path: ../protocol
  args: ^2.7.0
  path: ^1.9.1
  sqlite3: ^2.9.4
  uuid: ^4.5.1
  crypto: ^3.0.7

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0

executables:
  speeddial: speeddial
