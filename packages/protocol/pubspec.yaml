name: speeddial_protocol
description: Wire protocol models and JSON-RPC codec shared by the SpeedDial daemon and app.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.13.0

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
